services:
  - type: web
    name: new-fb-project
    env: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: SELENIUM_HEADLESS
        value: "True"
      - key: DEBUG
        value: "False"
